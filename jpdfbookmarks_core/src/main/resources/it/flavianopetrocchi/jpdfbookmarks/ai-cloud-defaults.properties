# Bundled defaults for cloud index extraction (optional). Maven filters from parent ai.cloud.* (-D…, CI secrets, ~/.m2/settings.xml).
# Use only Supabase anon JWT + public HTTPS URLs — never OpenAI secret keys here or in pom.xml (OpenAI stays in Edge secrets).
ai.cloud.process.index.url=${ai.cloud.process.index.url}
ai.cloud.anon.key=${ai.cloud.anon.key}
ai.cloud.check.payment.url=${ai.cloud.check.payment.url}
ai.cloud.fetch.full.url=${ai.cloud.fetch.full.url}
ai.cloud.create.checkout.url=${ai.cloud.create.checkout.url}
ai.cloud.stripe.prices.url=${ai.cloud.stripe.prices.url}
